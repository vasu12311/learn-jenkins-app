pipeline {
    agent any

    stages {

        stage('AZURE'){
            agent{
                docker{
                    image 'bitnami/azure-cli'
                    args "--entrypoint=''"
                }
            }

            steps{
                sh '''
                   sudo az version
                '''   
            }
        }     
    }
}