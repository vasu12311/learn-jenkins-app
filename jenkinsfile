pipeline {
    agent any

    stages {

        stage('build'){
            docker{
                image 'node:18-alpine'
            }
            steps{
                sh 'docker build -t my-playwright .'
            }
        }     
    }
}