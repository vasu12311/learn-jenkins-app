pipeline {
    agent any

    stages {

        stage('AZURE'){
            agent{
                docker{
                    image 'bitnami/azure-cli'
                    args "--user root --privileged"
                }
            }

            steps{
                sh '''
                   az version
                '''   
            }
        }     
    }
}